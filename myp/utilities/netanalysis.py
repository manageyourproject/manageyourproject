import networkx as nx

def calcUrg(projObj,subProj=False):
    # net = nx.DiGraph()
    # net.add_nodes_from(['start','a'])
    # net.add_edges_from([('start','a',{'weight':5})])
    # critPath = nx.algorithms.dag.dag_longest_path(net)
    pass



def buildMap(projObj):
    net = nx.DiGraph()
    if projObj['children
